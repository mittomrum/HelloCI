name: CheckIfLicensed 🔐

on:
  workflow_dispatch

jobs:
  checkLicense:
    name: Checking so that the UNITY_LICENSE has been added to git
    runs-on: ubuntu-latest
    steps: 
      - name: Fail - No License XX
        if: ${{ !startsWith(secrets.UNITY_LICENSE, '<') }}
        run: exit 1
